package compiler

import org.scalatest.{BeforeAndAfter, FunSuite}

class DataProgTest extends FunSuite with BeforeAndAfter {
  test("factorize locus") {
    val l: List[(String, Locus)] = List(("toto", V()), ("tata", V()), ("tutu", E()))

  }
}
