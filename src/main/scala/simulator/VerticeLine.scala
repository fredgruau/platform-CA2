package simulator

import scala.swing.Point

class VerticeLine(val vertices: Array[Array[Point]]) {

}

object VerticeLine {
  //if no point file are passed, it builds the perfect hexagonal grid.
  def apply(nbrows: Int, nbColumns: Int) = {

  }
}
